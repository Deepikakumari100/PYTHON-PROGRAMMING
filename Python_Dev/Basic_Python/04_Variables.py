# the name of the variable must be composed of upper-case or lower-case letters, digits, 
# and the character _ (underscore)
# the name of the variable must begin with a letter;
# the underscore character is a letter;
# upper- and lower-case letters are treated as different 
# the name of the variable must not be any of Python's reserved words 

var=25
print(var)

# You can use the print() function and combine text and 
# variables using the + operator to output strings and variables, e.g.:
var = "3.8.5"
print("Python version: " + var)

